{
  "name": "nexplore-technical-test",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "migrate": "npm run migrate --prefix backend",
    "start": "npm run install:all && npm run migrate && npm run build && concurrently \"npm run start --prefix backend\" \"npm run serve --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^9.0.1",
    "serve": "^14.2.4"
  }
}
